# Secrets — never commit
.env
.env.*
!.env.example

# Local data: append-only event log, vector index, caches
data/
*.db
*.db-wal
*.db-shm
*.sqlite
.cache/

# Node deps (created by --node-modules-dir for the transformers.js native runtime)
node_modules/

# Local embedding model weights
models/

# OS / editor
.DS_Store
*.swp
